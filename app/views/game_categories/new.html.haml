%fieldset
  %legend
    = t('generic.new_model', :name => GameCategory.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('back'), game_categories_path, :class => 'button'

