%fieldset
  %legend
    = t('generic.edit_model', :name => GameCategory.model_name.human)
    \-
    = @game_category.title

  = render 'form'

.row
  .small-12.columns.small-offset-1
    = link_to t('show'), @game_category, :class => 'button radius small'
    = link_to t('back'), game_categories_path, :class => 'button radius small'

