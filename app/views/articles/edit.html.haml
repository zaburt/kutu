%fieldset
  %legend
    = t('generic.edit_model', :name => Article.model_name.human)
    \-
    = @article.title

  = render 'form'

.row
  .small-12.columns.small-offset-1
    = link_to t('show'), @article, :class => 'button radius small'
    = link_to t('additional_pictures'), additional_pictures_article_path(@article), :class => 'button radius small'
    = link_to t('back'), articles_path, :class => 'button radius small'

