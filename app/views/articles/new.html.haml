%fieldset
  %legend
    = t('generic.new_model', :name => Article.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('back'), articles_path, :class => 'button'

