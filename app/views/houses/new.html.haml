%fieldset
  %legend
    = t('generic.new_model', :name => House.model_name.human)

  = render 'form'

  .row
    .small-12.columns.small-offset-1
      = link_to t('back'), houses_path, :class => 'button'

