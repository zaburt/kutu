%fieldset
  %legend
    = t('generic.new_model', :name => Game.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('back'), games_path, :class => 'button'

