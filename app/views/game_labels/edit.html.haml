%fieldset
  %legend
    = t('generic.edit_model', :name => GameLabel.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('show'), @game_label, :class => 'button'
      = link_to t('back'), game_labels_path, :class => 'button'

