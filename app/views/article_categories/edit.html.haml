%fieldset
  %legend
    = t('generic.edit_model', :name => ArticleCategory.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('show'), @article_category, :class => 'button'
      = link_to t('back'), article_categories_path, :class => 'button'

