%fieldset
  %legend
    = t('generic.edit_model', :name => City.model_name.human)

  = render 'form'

  .row
    .small-12.columns.small-offset-1
      = link_to t('show'), @city, :class => 'button'
      = link_to t('back'), cities_path, :class => 'button'

