%fieldset
  %legend
    = t('generic.edit_model', :name => User.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('show'), @user, :class => 'button'
      = link_to t('back'), users_path, :class => 'button'

