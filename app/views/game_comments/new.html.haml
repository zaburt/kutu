%fieldset
  %legend
    = t('generic.new_model', :name => GameComment.model_name.human)

  = render 'form'

  .grid-x
    .small-12.cell.small-offset-1
      = link_to t('back'), game_comments_path, :class => 'button'

